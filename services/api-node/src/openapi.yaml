openapi: 3.0.3
info:
  title: Mobile POS API
  version: 1.0.0
paths:
  /auth/login:
    post:
      summary: Login and get JWT
  /merchants:
    post:
      summary: Create merchant
  /sales:
    post:
      summary: Create checkout sale
  /webhooks/{provider}:
    post:
      summary: Payment webhook endpoint
